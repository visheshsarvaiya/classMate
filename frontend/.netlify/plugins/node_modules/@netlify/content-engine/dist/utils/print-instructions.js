"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printInstructions = printInstructions;
const chalk_1 = __importDefault(require("chalk"));
const prepare_urls_1 = require("./prepare-urls");
function printInstructions(program) {
    const urls = (0, prepare_urls_1.prepareUrls)(program.https ? `https` : `http`, program.host, program.port);
    const appName = program.sitePackageJson.name || `(Unnamed package)`;
    console.log(`\n${appName} is ready:`);
    if (urls.lanUrlForTerminal) {
        console.log(`  ${chalk_1.default.bold(`Local:`)}            ${urls.localUrlForTerminal}__graphql`);
        console.log(`  ${chalk_1.default.bold(`On Your Network:`)}  ${urls.lanUrlForTerminal}__graphql\n`);
    }
    else {
        console.log(`  ${urls.localUrlForTerminal}__graphql\n`);
    }
}
//# sourceMappingURL=print-instructions.js.map