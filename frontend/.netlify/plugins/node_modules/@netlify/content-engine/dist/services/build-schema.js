"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildSchema = buildSchema;
const schema_1 = require("../schema");
const reporter_1 = __importDefault(require("../reporter"));
async function buildSchema({ parentSpan, refresh, }) {
    if (refresh &&
        Boolean(process.env.GATSBY_EXPERIMENTAL_DISABLE_SCHEMA_REBUILD)) {
        return;
    }
    const activity = reporter_1.default.activityTimer(`building schema`, {
        parentSpan,
    });
    activity.start();
    await (0, schema_1.build)({ parentSpan: activity.span });
    activity.end();
}
//# sourceMappingURL=build-schema.js.map