export function delayedExit(ms = 100) {
    return new Promise(() => {
        setTimeout(() => {
            // leave a bit of time to print large errors before exiting
            process.exit(1);
        }, ms);
    });
}
//# sourceMappingURL=delayed-exit.js.map